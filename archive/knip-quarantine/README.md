# Knip Quarantine

This folder stores modules that are currently unused by the active app build,
but intentionally kept for reference instead of deletion.

Source paths are preserved under `archive/knip-quarantine/src/...` so files can
be restored with minimal friction if they become needed again.
